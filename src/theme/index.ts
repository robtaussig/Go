const theme = {
    presets: {
        centered: {
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
        },
    },
};

export default theme;

export type Theme = typeof theme;
